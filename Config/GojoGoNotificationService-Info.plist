<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Deliberately NOT inside GojoGoNotificationService/. That folder is a
     file-system-synchronized group, so everything in it gets target membership
     automatically — which for an Info.plist means it is copied as a resource
     *and* processed as the bundle's Info.plist, two tasks writing the same
     path. The build fails at planning time with "Multiple commands produce
     .../GojoGoNotificationService.appex/Info.plist". A file outside every
     synchronized folder has no membership at all, and is reachable only
     through INFOPLIST_FILE, which is exactly what is wanted here. -->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>GojoGo Notifications</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<!-- Both must track the app exactly: App Store Connect rejects a bundle
	     whose extension carries a different version from its host. The CI build
	     passes CURRENT_PROJECT_VERSION (and sometimes MARKETING_VERSION) as an
	     archive-wide xcarg, so substituting them here keeps the two in step
	     without a second place to remember to bump. -->
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.usernotifications.service</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).NotificationService</string>
	</dict>
</dict>
</plist>
