/**
 * Profile module — owns user profiles ({@code profile} schema).
 * Other modules interact only through {@link com.gojogo.profile.ProfileApi}.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Profile")
package com.gojogo.profile;
