/**
 * Social module — feed, posts, stories, comments, likes, follows
 * ({@code social} schema). Empty shell until Milestone 2.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Social")
package com.gojogo.social;
